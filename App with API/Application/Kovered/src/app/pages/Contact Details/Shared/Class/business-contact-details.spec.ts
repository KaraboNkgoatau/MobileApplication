import { BusinessContactDetails } from './business-contact-details';

describe('BusinessContactDetails', () => {
  it('should create an instance', () => {
    expect(new BusinessContactDetails()).toBeTruthy();
  });
});
